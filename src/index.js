import React from 'react';
import ReactDOM from 'react-dom';
import DarkMode from './Paginas/Dark-Mode-Web'


ReactDOM.render(
  <React.StrictMode>
    <DarkMode />
  </React.StrictMode>,
  document.getElementById('root')
);

